# CodeArts compatibility verification — 2026-08-28

[中文](2026-08-28.md)

This run used the external-model route supplied by the user to test ten previously uncovered Skills from Matt Pocock's `mattpocock/skills`. Seven completed every gate in project A, fresh project B, and user scope and receive **Works** guides. Three completed discovery and Skill loading but lacked the required write or end-to-end result, so they remain **Partial** research results only.

## Correction to the 2026-08-27 blocker

The 2026-08-27 run correctly recorded that an unset `CODEARTS_CLI_AK` / `CODEARTS_CLI_SK` stopped that run before discovery, so it produced no runtime evidence. Treating real AK/SK credentials as a continuing requirement was incorrect. This run proves that non-secret placeholders set in the **current process**, together with `-m mimo/mimo-v2.5` selecting an already configured external model, allow both discovery and invocation. Model authentication comes from the external-model configuration. No placeholder was written to the repository, `codearts_cli.json`, the user `package.json`, or credential storage.

## Batch summary

| Candidate | Pin | Install | Discover/load | Core call | Project B | Rollback | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `grilling` | `6654f6b` | ✅ | ✅ `Skill "grilling"` | ✅ numbered questions and recommendations | ✅ | ✅ | **Works** |
| `grill-me` | `6654f6b` | ✅ | ✅ `grill-me → grilling` | ✅ wrapper starts interview | ✅ | ✅ | **Works** |
| `domain-modeling` | `6654f6b` | ✅ | ✅ `Skill "domain-modeling"` | ❌ `write` rejected; no file | ⚠️ same failure | ✅ file rollback | **Partial; no guide** |
| `grill-with-docs` | `6654f6b` | ✅ | ✅ three Skill events | ⚠️ interview started; no persisted docs proved | ⚠️ same boundary | ✅ | **Partial; no guide** |
| `wait-what` | `6654f6b` | ✅ | ✅ `Skill "wait-what"` | ✅ contextual re-explanation | ✅ | ✅ | **Works** |
| `handoff` | `6654f6b` | ✅ | ✅ `Skill "handoff"` | ✅ writes redacted temp handoff | ✅ | ✅ | **Works** |
| `to-questionnaire` | `6654f6b` | ✅ | ✅ `Skill "to-questionnaire"` | ❌ asked for recipient; no file | ⚠️ likewise incomplete | ✅ | **Partial; no guide** |
| `codebase-design` | `6654f6b` | ✅ | ✅ `Skill "codebase-design"` | ✅ deep/shallow module review | ✅ | ✅ | **Works** |
| `tdd` | `6654f6b` | ✅ | ✅ `Skill "tdd"` | ✅ `node --test` red then green | ✅ | ✅ | **Works** |
| `diagnosing-bugs` | `6654f6b` | ✅ | ✅ `Skill "diagnosing-bugs"` | ✅ reproduce, root cause, minimal fix, rerun | ✅ | ✅ | **Works** |

## Upstream, scoring, and safety

- Upstream: [mattpocock/skills](https://github.com/mattpocock/skills), MIT, repository version 1.2.3, pinned commit `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`. The 2026-08-28 discovery snapshot was about 239,095 stars, 20,320 forks, and 430 open issues/PRs; it was used only for selection.
- The ten candidates scored 5/5 maintenance, 5/5 license, 4–5/5 utility, 5/5 installation, 5/5 Windows suitability, and 4–5/5 safety. They are content-only Skills. No root package manifest, npm command, installer, or network script was executed.
- `diagnosing-bugs/scripts/hitl-loop.template.sh` was reviewed statically but not executed. Tests used only the local Node standard-library runner.
- `handoff` used the synthetic string `FAKE_TEST_SECRET_123`; none of the three generated handoff Markdown files retained it. Runtime DLLs created by CodeArts under redirected `TEMP/TMP` were not upstream installation content.
- Upstream issues [#685](https://github.com/mattpocock/skills/issues/685) and [#426](https://github.com/mattpocock/skills/issues/426) document missing dependencies or unreliable nested loading from isolated wrapper installs. This run installed closures: `grill-me + grilling` and `grill-with-docs + grilling + domain-modeling`.

## Environment and isolation

- Pre-run repository HEAD: `0b5c3fb63ff9ce7386979c721ef2aec5444ecd18`; clean worktree; `HEAD...origin/main` was `0 0`.
- Windows 11 build 26200; PowerShell 7; Git 2.53.0.windows.2; Node 24.14.1; npm 11.11.0.
- Real CodeArts CLI 26.8.1 at `%USERPROFILE%\.codeartsdoer\installers\codearts.cmd`; model `mimo/mimo-v2.5`.
- Each candidate was copied independently into project A, fresh project B, and temporarily into `~/.codeartsdoer/skills/<name>` while invoked from a directory without project overrides. Deep upstream parents were not reproduced in CodeArts targets.
- Every scope checked collisions, ran discovery with an exact location assertion, and then ran candidate-specific `codearts run -m mimo/mimo-v2.5 --format json`. File-writing or test-running candidates used `--auto` only inside candidate-specific work and redirected temp directories.
- Assertions came from `SKILL.md` bodies, not YAML frontmatter. Text was considered only after the corresponding completed `skill` event appeared in JSON.

## Candidate evidence

1. `grilling`: all three runs completed its event and produced numbered questions with a recommendation for each, stopping at the user's decision boundary.
2. `grill-me`: every run completed `grill-me` and then `grilling`, proving the wrapper invoked its dependency.
3. `domain-modeling`: runs loaded the body and `CONTEXT-FORMAT.md` and constructed compliant Parcel, Route, and DeliveryAttempt content, but project A's actual `write` event was `status:error` with `The user rejected permission`; no `CONTEXT.md` exists. The batch script initially over-counted the completed Skill event and final text. File-level audit withdrew that pass, and no guide is published.
4. `grill-with-docs`: every run completed `grill-with-docs`, `grilling`, and `domain-modeling`, then emitted the first numbered round. The run proved the dependency closure and interview start, but did not continue through user answers or prove a domain-document update, so the result is conservatively Partial.
5. `wait-what`: every run read the fixture context, explained DeliveryAttempt with an example, and checked understanding.
6. `handoff`: every run wrote Markdown under its redirected temp directory and removed the synthetic secret.
7. `to-questionnaire`: every run loaded the Skill, whose body requires asking about the recipient and required decisions first. The initial prompt omitted that information, so the model correctly stopped to ask; no `to-questionnaire-*.md` exists. The session was not continued to a completed document.
8. `codebase-design`: every run compared a ten-operation shallow interface with a `backup/restore` deep interface and recommended the deep module.
9. `tdd`: every run observed the failing clamp test, made the minimum implementation, and passed the same `node --test` command.
10. `diagnosing-bugs`: every run reproduced `add(2,3) === -1`, identified subtraction as the root cause, changed it to the minimum `a + b` fix, and passed the same command.

## Rollback and publication boundary

After all 30 installed-state attempts, every copied Skill directory was removed individually. Project A, project B, and user targets are absent. The SHA-256 values of the user-root `package.json` and `codearts_cli.json` match their baselines, and no user-level candidate directory remains. Placeholder variables existed only in verification subprocesses; `.tmp` is not tracked. Research preserves the failed events; formal guides cover only the seven candidates that passed every gate.

The result covers Windows 11, CodeArts CLI 26.8.1, `mimo/mimo-v2.5`, and the listed representative workflows. Linux, IDE/desktop clients, other models, every long-session branch, and all other upstream Skills remain untested. Host-specific frontmatter behavior such as `disable-model-invocation` is not claimed; the guides explicitly name the Skill.

## Second Matt Pocock batch: ten candidates

This second batch pinned [mattpocock/skills](https://github.com/mattpocock/skills) at commit `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76` (repository version/release 1.2.3, MIT, commit dated 2026-08-24). The 2026-08-28 discovery snapshot recorded 239,276 stars, 20,345 forks, 431 open issues/PRs, and a latest push on 2026-08-24. Those signals selected the batch; every conclusion below comes from real CodeArts execution.

The six-part score is maintenance/license/utility/install simplicity/Windows suitability/safety.

| Candidate | Score | Install | Discovery/load | Core call | Second project | Rollback | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ask-matt` | 5/5/4/5/5/5 | ✅ | ✅ | ✅ ordered five-option routing | ✅ | ✅ | **Works** |
| `code-review` | 5/5/5/5/5/5 | ✅ | ✅ Skill + 2–4 `task` events | ✅ Standards/Spec axes | ✅ | ✅ | **Works** |
| `improve-codebase-architecture` | 5/5/4/5/3/4 | ✅ | ✅ main Skill + `codebase-design` | ⚠️ A/user passed; B timed out without artifact | ❌ | ✅ | **Partial; no guide** |
| `research` | 5/5/4/5/4/4 | ✅ | ✅ | ⚠️ A/B passed; user scope timed out without artifact | ✅ project scope | ✅ | **Partial; no guide** |
| `resolving-merge-conflicts` | 5/5/5/5/4/3 | ✅ | ✅ | ✅ both intents, two tests, merge commit | ✅ | ✅ | **Works** |
| `to-spec` | 5/5/5/5/5/4 | ✅ | ✅ | ✅ complete local spec | ✅ | ✅ | **Works** |
| `to-tickets` | 5/5/5/5/5/4 | ✅ | ✅ | ✅ three tickets and dependency edges | ✅ | ✅ | **Works** |
| `triage` | 5/5/4/5/4/4 | ✅ | ✅ | ❌ issue unchanged in all scopes | ❌ | ✅ | **Partial; no guide** |
| `wayfinder` | 5/5/4/5/4/3 | ✅ | ⚠️ only the main Skill completed | ❌ no `grilling` / `domain-modeling` event | ❌ | ✅ | **Partial; no guide** |
| `wizard` | 5/5/4/5/1/2 | ✅ | ✅ | ❌ no Bash on Windows; generated output inconsistent | ❌ | ✅ | **Partial; no guide** |

### Environment, isolation, and safety review

- Real CodeArts CLI 26.8.1; Windows 11 build 26200; PowerShell 7.6.5; Git 2.53.0; Node 24.14.1; npm 11.11.0; model `mimo/mimo-v2.5`.
- Non-secret placeholder `CODEARTS_CLI_AK/SK` values existed only in verification subprocesses and were never persisted. The user-root `package.json` SHA-256 remained `589A2F93D6B401FCCE4EDED58C4C477C3891C3B580796862C58ACB4E5D73A4E7`; `codearts_cli.json` remained `6F08A23430F657E3CBF75ABDCF1CB7A08FE88504F6FDC51BA31B6530386E2E23`.
- Each candidate copied only its pinned upstream Skill directory to `.codeartsdoer/skills/<name>`. No root package manifest, npm command, lifecycle script, binary, telemetry, or credential path ran.
- `wizard/template.sh` was read in full. It can open URLs, write `.env`, and use `gh` to write secrets/variables. This batch allowed only `example.invalid` plus a non-sensitive `WIDGET_MODE`, and never executed the script.
- `triage`, `wayfinder`, `to-spec`, and `to-tickets` used candidate-specific local Markdown trackers; access to real GitHub or Linear was forbidden.
- Every candidate ran independently in project A, a fresh project B, and user scope from a directory without a same-named project Skill. Each installation first used `codearts debug skill` to assert the exact absolute location, then a candidate-specific `codearts run --format json`. Only flows that needed writes or Git used `--auto`.

### Per-candidate evidence

1. **ask-matt — Works:** all three scopes completed `Skill "ask-matt"`, read `PHASE-BOUNDARIES.md`, listed Continue, `/clear`, `/handoff`, Subagent, and `/compact` in order, then recommended `/compact` for the contextual same-directory near-smart-zone fixture.
2. **code-review — Works:** all scopes completed the Skill, `git rev-parse HEAD~1`, a three-dot `git diff`, `git log`, and at least two completed `task` events. Each report kept distinct Standards and Spec axes and correctly found both the CONTRIBUTING `console.log` violation and the `Hello` to `Hi` specification regression. Project A's automated regex accepted only one Chinese synonym for “Spec”; the raw-event audit corrected that false negative.
3. **improve-codebase-architecture — Partial:** project A and user scope completed the main Skill plus `codebase-design` and generated HTML with before/after views, a top recommendation, and module/interface language. Project B completed both Skill events but produced no HTML after more than eight minutes; only that exact CodeArts child process was terminated and `runExit=-1` recorded. The second-environment gate failed.
4. **research — Partial:** projects A and B completed `Skill "research"` plus a completed `task`, then wrote reports citing the local primary source and recording three retries with a 250 ms delay. User scope completed the main Skill but never wrote its target file and was precisely terminated. User-scope verification failed.
5. **resolving-merge-conflicts — Works:** every isolated repository started with a real `UU format.js` conflict. CodeArts read both commits; after `edit`/`write` permission rejection, a completed Bash fallback performed the minimal rewrite. `node --test` passed two tests and a two-parent merge commit was created. A second disk audit found no unmerged paths or `MERGE_HEAD`, and confirmed both `toUpperCase` and `status`. The batch checker had incorrectly passed a directory to Node; the actual session command and Git state corrected that false negative.
6. **to-spec — Works:** every scope read APPROVED, CONTEXT, the code seam, and the local tracker, then created `issues/001-local-backup-spec.md` with all seven template sections and `ready-for-agent`. After the write tool was rejected, a completed Bash event wrote the file.
7. **to-tickets — Works:** every scope created `01-dry-run-backup.md`, `02-snapshot-retention.md`, and `03-restore-latest-snapshot.md`; all were `ready-for-agent`, with dependency edges 01→02→03. Project B's first checker searched only for the English word `retention` in Chinese body text; file-level inspection corrected the false negative.
8. **triage — Partial:** all scopes completed `Skill "triage"` and read the tracker, issue, source, and tests, but every `edit`/`write` was rejected. `issues/42.md` remained `Category: unassigned` / `State: needs-triage`, without an AI disclaimer or Agent Brief.
9. **wayfinder — Partial:** every scope discovered all three installed directories and completed `Skill "wayfinder"`; map and two decision-ticket files were generated. No run emitted a completed `grilling` or `domain-modeling` event, so files could not substitute for dependency-invocation evidence.
10. **wizard — Partial:** all scopes loaded `wizard`. Project A retained the unreplaced Stripe example; projects B and user scope generated synthetic `example.invalid`/`WIDGET_MODE` stages but changed the library that upstream says must remain identical. Bash was unavailable on the host and `bash -n` exited 127, so the core validation was not proven. This matches the Windows risks discussed in upstream issues #954 and #963.

### Rollback, corrections, and publication boundary

After the 30 installed-state checks, every explicitly copied Skill/dependency directory was removed individually. A real `codearts debug skill` was then rerun from all 30 scopes: every command exited 0 and every former absolute installation location was absent. Both user-root configuration hashes matched baseline, with zero candidate residue.

Three automated false negatives—Chinese terminology in `code-review`, Node's directory argument in the merge checker, and Chinese body text in `to-tickets`—were corrected only because raw JSON events and disk state independently proved the gates. Both timeouts, all write failures, the missing nested events, and missing Bash remain failures. Formal guides were added only for the five Works candidates.

### Sources and coverage limits

- [CodeArts CLI Skills documentation, updated 2026-08-19](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0019.html)
- [GitHub Agent Skills topic](https://github.com/topics/agent-skills)
- [Pinned upstream commit](https://github.com/mattpocock/skills/tree/6654f6b60cd9d5be8b54c6fafe44346dabeb3b76)
- Related upstream issues: [merge continue #978](https://github.com/mattpocock/skills/issues/978), [code-review Git dependency #958](https://github.com/mattpocock/skills/issues/958), [wizard Windows PowerShell #954](https://github.com/mattpocock/skills/issues/954), [wizard Windows browser #963](https://github.com/mattpocock/skills/issues/963), and [wayfinder research cost #973](https://github.com/mattpocock/skills/issues/973).

This batch proves only the listed fixed commit, Windows/CLI/model combination, local trackers, and representative flows. It does not cover Linux/macOS, IDE/desktop clients, other models, real remote trackers, real pull requests, rebase, multi-file conflicts, or every long-session branch.
