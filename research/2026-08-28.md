# CodeArts 兼容验证记录 — 2026-08-28

[English](2026-08-28.en.md)

本轮按用户提供的外部模型路径，实际验证 Matt Pocock `mattpocock/skills` 中 10 个此前未覆盖的 Skills。7 项在项目 A、全新项目 B 和无项目同名覆盖的个人级目录完成全部门槛并发布 **Works** 指南；3 项完成发现和 Skill 加载，但写入或端到端结果不足，只保留 **Partial** 研究结论。

## 对 2026-08-27 阻塞结论的纠正

2026-08-27 的运行只观察到未设置 `CODEARTS_CLI_AK` / `CODEARTS_CLI_SK` 时 CLI 在发现前停止，因而当时没有运行时证据；该历史记录中的“当次未验证”事实不变，但把缺少真实 AK/SK 当作后续阻塞是不正确的。本轮实测证明：在**当前进程**给两个变量设置非敏感占位值，并用 `-m mimo/mimo-v2.5` 显式选择已配置的外部模型后，`codearts debug skill` 和 `codearts run` 均正常工作。模型鉴权来自外部模型配置；占位值未写入仓库、`codearts_cli.json`、用户 `package.json` 或凭据存储。

## 批次汇总

| 候选 | 固定版本 | 安装 | 发现/加载 | 核心调用 | 第二项目 | 回滚 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `grilling` | `6654f6b` | ✅ | ✅ `Skill "grilling"` | ✅ 编号问题与推荐答案 | ✅ | ✅ | **Works** |
| `grill-me` | `6654f6b` | ✅ | ✅ `grill-me → grilling` | ✅ 包装器启动访谈 | ✅ | ✅ | **Works** |
| `domain-modeling` | `6654f6b` | ✅ | ✅ `Skill "domain-modeling"` | ❌ `write` 被拒，未生成文件 | ⚠️ 同样失败 | ✅ 文件回滚 | **Partial；无指南** |
| `grill-with-docs` | `6654f6b` | ✅ | ✅ 三个 Skill 事件 | ⚠️ 启动访谈，未证明文档落盘 | ⚠️ 同边界 | ✅ | **Partial；无指南** |
| `wait-what` | `6654f6b` | ✅ | ✅ `Skill "wait-what"` | ✅ 结合上下文重讲术语 | ✅ | ✅ | **Works** |
| `handoff` | `6654f6b` | ✅ | ✅ `Skill "handoff"` | ✅ 写临时交接并脱敏 | ✅ | ✅ | **Works** |
| `to-questionnaire` | `6654f6b` | ✅ | ✅ `Skill "to-questionnaire"` | ❌ 先追问收件人，未写文件 | ⚠️ 同样未完成 | ✅ | **Partial；无指南** |
| `codebase-design` | `6654f6b` | ✅ | ✅ `Skill "codebase-design"` | ✅ 深/浅模块评审 | ✅ | ✅ | **Works** |
| `tdd` | `6654f6b` | ✅ | ✅ `Skill "tdd"` | ✅ `node --test` 先红后绿 | ✅ | ✅ | **Works** |
| `diagnosing-bugs` | `6654f6b` | ✅ | ✅ `Skill "diagnosing-bugs"` | ✅ 复现、根因、最小修复、复跑 | ✅ | ✅ | **Works** |

## 上游、评分与安全审查

- 上游：[mattpocock/skills](https://github.com/mattpocock/skills)，MIT，仓库版本 `1.2.3`，固定 Commit `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`。2026-08-28 发现快照为约 239,095 Stars、20,320 Forks、430 Open issues/PRs；快照只用于候选选择。
- 10 项维护 5/5、许可证 5/5、实用性 4–5/5、安装 5/5、Windows 5/5、安全 4–5/5；都属于内容型 Skill，直接复制完整候选目录，不运行根 `package.json`、npm、安装器或网络脚本。
- `diagnosing-bugs/scripts/hitl-loop.template.sh` 已静态审阅，但本轮没有执行。测试只运行本地 Node 标准库 `node --test`。
- `handoff` 使用伪造字符串 `FAKE_TEST_SECRET_123` 检查脱敏；三个生成的交接 Markdown 均不含该字符串。CodeArts 自身在定向 `TEMP/TMP` 下产生的运行时 DLL 不属于上游安装内容。
- 上游 issue [#685](https://github.com/mattpocock/skills/issues/685) 与 [#426](https://github.com/mattpocock/skills/issues/426) 说明孤立安装包装型 Skill 会漏依赖或嵌套加载不稳定。本轮按依赖闭包安装：`grill-me + grilling`；`grill-with-docs + grilling + domain-modeling`。

## 环境与隔离方法

- 本地仓库运行前 HEAD `0b5c3fb63ff9ce7386979c721ef2aec5444ecd18`；工作树干净，`HEAD...origin/main` 为 `0 0`。
- Windows 11 Build 26200；PowerShell 7；Git 2.53.0.windows.2；Node 24.14.1；npm 11.11.0。
- 真实 CodeArts CLI 26.8.1：`%USERPROFILE%\.codeartsdoer\installers\codearts.cmd`；模型 `mimo/mimo-v2.5`。
- 每个候选分别复制到 `.tmp/verification/2026-08-28-matt/<slug>-a/.codeartsdoer/skills`、独立 `<slug>-b`，以及临时的 `~/.codeartsdoer/skills/<name>`。深层上游目录没有带入目标层级。
- 每个范围都先检查同名冲突，运行 `codearts debug skill` 并核对目标绝对 location，再运行候选专用 `codearts run -m mimo/mimo-v2.5 --format json`。写文件或执行测试的候选额外使用 `--auto`，且工作目录和 `TEMP/TMP` 均定向到候选隔离目录。
- 通过断言来自 `SKILL.md` 正文，不依赖运行时不会返回的 YAML frontmatter。模型文本只有在 JSON 中出现对应 completed `skill` 事件后才参与结果判断。

## 逐项核心证据

1. `grilling`：三次均输出 `Skill "grilling"`，随后出现 `Q1` 等编号问题，每题有推荐答案并停在用户决策边界。
2. `grill-me`：三次均先完成 `Skill "grill-me"`，再完成 `Skill "grilling"`；证明短包装器实际调用依赖，不是模型自行模仿。
3. `domain-modeling`：三次均加载正文并读取 `CONTEXT-FORMAT.md`，也构造出包含 Parcel、Route、DeliveryAttempt 的合规内容；但项目 A 的实际 `write` 事件明确为 `status:error`、`The user rejected permission`，磁盘没有 `CONTEXT.md`。批量脚本原先只检查完成 Skill 事件和最终文本，曾误报核心调用成功；二次文件审计将其撤回。其余范围没有可接受的落盘证据，故不发布指南。
4. `grill-with-docs`：三次均依次完成 `grill-with-docs`、`grilling`、`domain-modeling`，随后输出首轮编号问题；但本轮只证明包装器依赖闭包和访谈启动，没有完成一轮用户回答，也没有证明领域文档更新，故保守记为 Partial。
5. `wait-what`：三次均读取夹具 `CONTEXT.md`，用具体例子解释 DeliveryAttempt 并检查理解。
6. `handoff`：三次均在候选专属临时目录写入 Markdown；伪造 secret 均被移除。
7. `to-questionnaire`：三次均加载 Skill，但正文要求先询问收件人和所需决策；初始提示没有提供这些信息，模型正确停下来追问，磁盘没有 `to-questionnaire-*.md`。没有续接会话完成问卷，故核心流程未通过。
8. `codebase-design`：三次均比较十个低层文件操作的浅接口与 `backup/restore` 深接口，并推荐深模块。
9. `tdd`：三次均先运行失败的 `clamp.test.js`，再把未实现函数改为最小 clamp 实现，用同一 `node --test` 通过。
10. `diagnosing-bugs`：三次均用 `node --test` 复现 `add(2,3)` 返回 `-1`，把根因定位到减法，实现最小 `a + b` 修复并复跑通过。

## 回滚与发布边界

30 次安装态尝试后，所有明确复制的 Skill 目录均逐项删除；项目 A、项目 B 与用户级目标均不存在。用户根 `package.json` 与 `codearts_cli.json` 的 SHA-256 前后相同，且没有残留 10 个候选的用户级目录。占位环境变量只存在于验证子进程。`.tmp` 不纳入 Git。研究保留失败事件，正式指南只覆盖 7 个通过全部门槛的候选。

本轮仅证明 Windows 11、CodeArts CLI 26.8.1、`mimo/mimo-v2.5` 和列出的代表流程。没有验证 Linux、IDE/桌面端、其他模型、所有长会话分支或上游全部 Skills。`disable-model-invocation` 等宿主 frontmatter 行为没有作为兼容结论；指南要求显式点名 Skill。


## 第二批：Matt Pocock 十项验证

本节记录同日第二个独立批次。候选来自 GitHub Agent Skills 热门主题中的 `mattpocock/skills`，且均未出现在此前 research 或 guides。上游固定 Commit `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`（MIT，仓库版本 1.2.3；Commit 日期 2026-08-24）。2026-08-28 快照为 239,276 Stars、20,345 Forks、431 Open issues/PRs，最新 Release `v1.2.3`（2026-08-06）。

### 批次汇总与评分

评分顺序为维护/许可证/实用性/安装简洁度/Windows 适配/安全，满分 5。

| 候选 | 评分 | 安装 | 发现/加载 | 核心调用 | 第二项目 | 回滚 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ask-matt` | 5/5/4/5/5/5 | ✅ | ✅ | ✅ 五选项有序路由 | ✅ | ✅ | **Works** |
| `code-review` | 5/5/5/5/5/5 | ✅ | ✅ Skill + 2–4 个 `task` | ✅ Standards/Spec 双轴 | ✅ | ✅ | **Works** |
| `improve-codebase-architecture` | 5/5/4/5/3/4 | ✅ | ✅ 主 Skill + `codebase-design` | ⚠️ A/个人成功，B 无产物超时 | ❌ | ✅ | **Partial；无指南** |
| `research` | 5/5/4/5/4/4 | ✅ | ✅ | ⚠️ A/B 成功，个人级无产物超时 | ✅ 项目级 | ✅ | **Partial；无指南** |
| `resolving-merge-conflicts` | 5/5/5/5/4/3 | ✅ | ✅ | ✅ 两意图、两测试、merge commit | ✅ | ✅ | **Works** |
| `to-spec` | 5/5/5/5/5/4 | ✅ | ✅ | ✅ 完整本地 spec | ✅ | ✅ | **Works** |
| `to-tickets` | 5/5/5/5/5/4 | ✅ | ✅ | ✅ 三个工单与阻塞边 | ✅ | ✅ | **Works** |
| `triage` | 5/5/4/5/4/4 | ✅ | ✅ | ❌ 三范围均未更新 issue | ❌ | ✅ | **Partial；无指南** |
| `wayfinder` | 5/5/4/5/4/3 | ✅ | ⚠️ 只完成主 Skill | ❌ 缺 `grilling` / `domain-modeling` 事件 | ❌ | ✅ | **Partial；无指南** |
| `wizard` | 5/5/4/5/1/2 | ✅ | ✅ | ❌ Windows 无 Bash，生成结果不一致 | ❌ | ✅ | **Partial；无指南** |

### 环境、隔离与安全审查

- 真实 CodeArts CLI 26.8.1，Windows 11 Build 26200，PowerShell 7.6.5，Git 2.53.0，Node 24.14.1，npm 11.11.0，模型 `mimo/mimo-v2.5`。
- 当前进程用非敏感占位 `CODEARTS_CLI_AK/SK` 通过 CLI 前置检查；占位值没有写入文件。用户根 `package.json` 哈希始终为 `589A2F93D6B401FCCE4EDED58C4C477C3891C3B580796862C58ACB4E5D73A4E7`，`codearts_cli.json` 始终为 `6F08A23430F657E3CBF75ABDCF1CB7A08FE88504F6FDC51BA31B6530386E2E23`。
- 十项均只复制固定的上游 Skill 目录到 `.codeartsdoer/skills/<name>`；没有运行根 `package.json`、npm、生命周期脚本、二进制、遥测或凭据流程。
- `wizard/template.sh` 已完整审阅；它可打开 URL、写 `.env` 和用 `gh` 写 secrets/variables。本批只允许 `example.invalid` 与非敏感 `WIDGET_MODE`，且绝不执行脚本。
- `triage`、`wayfinder`、`to-spec`、`to-tickets` 使用候选专属本地 Markdown tracker，禁止访问真实 GitHub/Linear。
- 每项分别运行项目 A、全新项目 B、无项目同名覆盖的个人级；安装后先 `codearts debug skill` 核对绝对 location，再运行候选专用 `codearts run --format json`。需要写入/Git 的流程才加 `--auto`。

### 逐项证据

1. **ask-matt — Works**：三个范围均完成 `Skill "ask-matt"`，读取 `PHASE-BOUNDARIES.md`，依次列出 Continue、`/clear`、`/handoff`、Subagent、`/compact`，并基于“上下文相关、同目录、接近 smart zone”推荐 `/compact`。
2. **code-review — Works**：三个范围均完成 Skill、`git rev-parse HEAD~1`、三点 `git diff`、`git log` 和至少两个 completed `task` 事件。报告分别保留 Standards/标准轴与 Spec/规范轴，并准确指出 `console.log` 违反 CONTRIBUTING 及 `Hello`→`Hi` 规格回归。项目 A 的自动正则只接受“规格”而未接受“规范”，二次原始事件审计纠正为通过。
3. **improve-codebase-architecture — Partial**：项目 A 和个人级均完成主 Skill 与 `codebase-design`，生成含 before/after、Top recommendation、module/interface 词汇的 HTML。项目 B 完成两个 Skill 事件后超过 8 分钟仍无 HTML，精确终止该 CodeArts 子进程并记录 `runExit=-1`；第二环境门槛失败。
4. **research — Partial**：项目 A/B 均完成 `Skill "research"`、completed `task`，写出引用本地 primary source 的报告，正确记录 3 次重试和 250 ms。个人级完成主 Skill 后长时间无目标文件，精确终止；个人范围失败。
5. **resolving-merge-conflicts — Works**：三个独立仓库均从真实 `UU format.js` 开始。CodeArts 读取两侧提交，`edit`/`write` 被权限层拒绝后通过 completed Bash 最小重写，`node --test` 两项通过，完成双亲 merge commit。二次磁盘审计确认无未合并路径、无 `MERGE_HEAD`，且结果同时包含 `toUpperCase` 与 `status`。批脚本曾错误执行 `node --test <目录>` 并假阴性，已按真实会话命令与 Git 状态纠正。
6. **to-spec — Works**：三个范围均读取 APPROVED、CONTEXT、代码 seam 与本地 tracker，实际生成 `issues/001-local-backup-spec.md`；七个模板章节和 `ready-for-agent` 全部存在。写工具被拒后通过 completed Bash 写入。
7. **to-tickets — Works**：三个范围均生成 `01-dry-run-backup.md`、`02-snapshot-retention.md`、`03-restore-latest-snapshot.md`，状态均为 `ready-for-agent`，阻塞边为 01→02→03。项目 B 的首轮脚本只扫描正文中的英文 `retention` 而漏掉中文正文；文件级审计纠正为通过。
8. **triage — Partial**：三个范围都完成 `Skill "triage"` 并读取 tracker、issue、源码和测试，但 `edit`/`write` 事件均被拒，`issues/42.md` 始终保持 `Category: unassigned` / `State: needs-triage`，没有 AI disclaimer 或 Agent Brief。
9. **wayfinder — Partial**：三个范围均发现全部三个安装目录并完成 `Skill "wayfinder"`，也生成 map 与两个 decision-ticket 文件；但运行时从未出现 completed `grilling` 或 `domain-modeling` 事件。按组合 Skill 门槛，文件不能替代依赖调用证据。
10. **wizard — Partial**：三个范围均加载 `wizard`。项目 A 保留了未经替换的 Stripe 示例；项目 B/个人级虽生成 `example.invalid` 的 `WIDGET_MODE` 阶段，却改写了上游要求保持一致的 library。主机没有可用 Bash，`bash -n` 退出 127；因此不能证明核心验证，且上游 issue #954/#963 的 Windows 风险与实测一致。

### 回滚、纠错与发布边界

30 次安装态验证后，每个明确复制的 Skill/依赖目录均逐项删除。随后从 30 个范围分别重新运行真实 `codearts debug skill`，退出码全部为 0，原安装绝对 location 全部消失。用户根两个配置文件哈希恢复基线，用户候选残留为零。

自动断言的三个假阴性（`code-review` 中文同义词、merge 的 Node 目录参数、`to-tickets` 中文正文）只在原始 JSON 事件和磁盘状态足以证明时纠正；两个超时、三项写失败、缺嵌套事件和 Bash 缺失均保留为失败。正式指南只新增五个 Works 候选。

### 来源与未覆盖范围

- [CodeArts CLI Skills（2026-08-19 更新）](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0019.html)
- [GitHub Agent Skills Topic](https://github.com/topics/agent-skills)
- [上游固定提交](https://github.com/mattpocock/skills/tree/6654f6b60cd9d5be8b54c6fafe44346dabeb3b76)
- 相关上游问题：[merge continue #978](https://github.com/mattpocock/skills/issues/978)、[code-review Git 依赖 #958](https://github.com/mattpocock/skills/issues/958)、[wizard Windows PowerShell #954](https://github.com/mattpocock/skills/issues/954)、[wizard Windows browser #963](https://github.com/mattpocock/skills/issues/963)、[wayfinder research cost #973](https://github.com/mattpocock/skills/issues/973)。

本批只证明上述 Windows、CLI、模型、固定提交、本地 tracker 和代表流程。未验证 Linux/macOS、IDE/桌面端、其他模型、真实远程 tracker、真实 PR、rebase、多文件冲突或所有长会话分支。
