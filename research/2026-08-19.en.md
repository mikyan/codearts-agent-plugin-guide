# CodeArts compatibility verification — 2026-08-19

[简体中文](2026-08-19.md)

This record extends the [2026-08-18 project-scope verification](2026-08-18.en.md). It tests whether Ponytail and Superpowers can be installed in the current user's CodeArts directory and used from projects with no project-scoped configuration.

## Conclusion

| Project | Pinned version | Project scope | User scope | Compatibility |
| --- | --- | --- | --- | --- |
| [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) | 4.9.0, `0a4dd63ad4541f4f655c4108a295916f3c1d8fda` | Verified | Verified | Adapter Required |
| [obra/superpowers](https://github.com/obra/superpowers) | 6.3.0, `b36e0829c6d0140e93cfef2ca599b1b07d4a7797` | Verified | Verified | Adapter Required |

CodeArts officially supports project `.codeartsdoer/skills` and user `~/.codeartsdoer/skills`, with project skills taking priority when names match. Plugins likewise support project and user directories. “User scope” means available to all CodeArts projects for the current user, not every user on the machine.

## Verification environment

- Windows 11 Build 26200
- CodeArts CLI 26.8.1
- PowerShell 7.6.5
- Node.js 24.14.1, npm 11.11.0, Git 2.53.0
- Custom model: `mimo/mimo-v2.5`
- Credentials were inherited only by CodeArts and were not printed or written to the repository
- Real calls used `codearts run --format json --sandbox`, without `--auto`

CodeArts desktop/IDE and Linux were not tested in this run.

## Problem discovered

Before testing, the user-root `%USERPROFILE%/.codeartsdoer/package.json` already contained CodeArts' bundled `@opencode-ai/plugin@26.8.1`. Merging third-party dependencies into this file caused both system npm and CodeArts automatic reification to try resolving that internal version from the public npm registry, which returned `ETARGET`.

User scope therefore cannot safely reuse the project dependency layout. The verified structure is:

```text
~/.codeartsdoer/
  plugins/<project>.ts
  vendor/<project>/
    package.json
    package-lock.json
    node_modules/
  skills/<copied-skill>/
```

Each project has an isolated `vendor` manifest. Its plugin loads the upstream entrypoint through a fixed path relative to `~/.codeartsdoer/plugins`, while skills are copied into CodeArts' native user directory. The user-root `package.json` remains unchanged.

## Ponytail verification

- `vendor/ponytail/package.json` pinned only `@dietrichgebert/ponytail` 4.9.0.
- Installation used `npm install --ignore-scripts --no-audit --no-fund`.
- `plugins/ponytail.ts` loaded upstream `.opencode/plugins/ponytail.mjs`.
- Six skill directories were copied into user scope.
- CodeArts ran from an independent directory with no project `.codeartsdoer`.
- `ponytail-help` produced a completed native `skill` event with its base directory under user skills and returned:

```text
Lite, Full, Ultra
```

## Superpowers verification

- `vendor/superpowers/package.json` pinned Git tag `v6.3.0`.
- Installation used `npm install --ignore-scripts --no-audit --no-fund`.
- `plugins/superpowers.ts` loaded upstream `.opencode/plugins/superpowers.js`.
- Fourteen skill directories were copied into user scope.
- CodeArts ran from the same independent directory with no project configuration.
- `systematic-debugging` produced a completed native `skill` event with its base directory under user skills and returned:

```text
NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST
```

## Rollback verification

The Ponytail and Superpowers user plugins, isolated `vendor` directories, and corresponding skill directories were completely moved out of the user CodeArts directory. Checks then confirmed that:

- neither third-party skill set remained discoverable;
- the SHA-256 of user-root `package.json` exactly matched its pre-test value;
- `codearts_cli.json`, credentials, and unrelated CodeArts plugins were unchanged.

Project installation had already been reproduced and rolled back in two isolated projects. The formal guides now document installation and removal for both scopes.

## Published guides

- [Ponytail — 中文](../guides/ponytail/README.md) · [English](../guides/ponytail/README.en.md)
- [Superpowers — 中文](../guides/superpowers/README.md) · [English](../guides/superpowers/README.en.md)

## First-party sources

- [CodeArts CLI Skills](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0019.html)
- [CodeArts CLI Hooks](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0018.html)

---

## Batch verification of ten popular Agent Skills repositories

Building on the Ponytail and Superpowers adapter findings, this run selected ten popular repositories using a GitHub star snapshot from 2026-08-19. Only one representative skill was selected from each repository; conclusions do not extend to other skills, plugins, or clients in those repositories.

| Upstream | Star snapshot | Pinned commit | Selected skill | Result | Guide |
| --- | ---: | --- | --- | --- | --- |
| [anthropics/skills](https://github.com/anthropics/skills) | 170,227 | `0a64e39` | `frontend-design` | Works | [中文](../guides/anthropic-skills/README.md) · [English](../guides/anthropic-skills/README.en.md) |
| [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills) | 88,268 | `df1edb2` | `code-simplification` | Works | [中文](../guides/addyosmani-agent-skills/README.md) · [English](../guides/addyosmani-agent-skills/README.en.md) |
| [kepano/obsidian-skills](https://github.com/kepano/obsidian-skills) | 46,655 | `a1dc48e` | `obsidian-markdown` | Works | [中文](../guides/obsidian-skills/README.md) · [English](../guides/obsidian-skills/README.en.md) |
| [github/awesome-copilot](https://github.com/github/awesome-copilot) | 37,982 | `318066d` | `commit-message-storyteller` | Works | [中文](../guides/github-awesome-copilot/README.md) · [English](../guides/github-awesome-copilot/README.en.md) |
| [blader/humanizer](https://github.com/blader/humanizer) | 36,342 | `ebf637b` | `humanizer` | Adapter Required | [中文](../guides/humanizer/README.md) · [English](../guides/humanizer/README.en.md) |
| [K-Dense-AI/scientific-agent-skills](https://github.com/K-Dense-AI/scientific-agent-skills) | 33,819 | `9e8b0cb` | `experimental-design` | Partial | [中文](../guides/scientific-agent-skills/README.md) · [English](../guides/scientific-agent-skills/README.en.md) |
| [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills) | 30,164 | `b8caa26` | `vercel-react-best-practices` | Works | [中文](../guides/vercel-agent-skills/README.md) · [English](../guides/vercel-agent-skills/README.en.md) |
| [phuryn/pm-skills](https://github.com/phuryn/pm-skills) | 25,391 | `18468a9` | `prioritization-frameworks` | Works | [中文](../guides/pm-skills/README.md) · [English](../guides/pm-skills/README.en.md) |
| [openai/skills](https://github.com/openai/skills) | 25,018 | `49f948f` | `security-best-practices` | Works | [中文](../guides/openai-skills/README.md) · [English](../guides/openai-skills/README.en.md) |
| [google/skills](https://github.com/google/skills) | 18,462 | `65ef106` | `gke-manifest-generation` | Partial | [中文](../guides/google-skills/README.md) · [English](../guides/google-skills/README.en.md) |

Stars are only a popularity snapshot for candidate selection, not a quality or security guarantee. Every installation and guide is pinned to the listed commit rather than a moving branch.

### Reused verification method

This run actively followed the repository's [CodeArts plugin compatibility skill](../skills/codearts-plugin-compatibility/SKILL.md):

1. Inspect the pinned `SKILL.md`, local resources, license, and executable scripts.
2. Copy only the selected skill into an isolated project's `.codeartsdoer/skills/<skill-name>` without running upstream installers or installing unrelated dependencies.
3. Check its name and resolved path with `codearts debug skill`, then require an explicit skill call in a real `codearts run --format json --sandbox --model mimo/mimo-v2.5` session.
4. Reproduce the final process from zero in a second isolated project.
5. Install the same skills under `~/.codeartsdoer/skills/<skill-name>` and call them again from an independent directory with no project override.
6. Remove only the test targets, confirm neither scope discovers the skills, and confirm the user-root `package.json` hash is unchanged.

All ten candidates completed discovery, a successful `skill` event, and an attributable result. The OpenAI case also completed one relative-path `read` inside the skill. No AK, SK, or model API key was written to command output or the repository. The custom-provider sessions used non-secret AK/SK placeholders; the actual model credential remained in the local Provider configuration in `codearts_cli.json`.

### Representative task results

| Skill | Attributable acceptance result |
| --- | --- |
| `frontend-design` | Returned four requested design dimensions and at least four six-digit hex colors for a tea timer. |
| `code-simplification` | Replaced a branching JavaScript validator with a direct Boolean return. |
| `obsidian-markdown` | Produced an Obsidian note with YAML frontmatter, a `[[Roadmap]]` link, and a Tip callout. |
| `commit-message-storyteller` | Produced a `perf(cache)` Conventional Commit explaining repeated work and latency. |
| `humanizer` | Preserved factual year `2024` while removing the specified hype and stock phrases. |
| `experimental-design` | Identified independent sample size 4 and experimental unit mouse rather than treating 80 cells as independent samples. |
| `vercel-react-best-practices` | Used `Promise.all` to remove independent sequential awaits while preserving the return shape. |
| `prioritization-frameworks` | Correctly calculated RICE score `25`. |
| `security-best-practices` | Read the Express security reference after the skill call and returned `helmet()`. |
| `gke-manifest-generation` | Generated YAML with namespace, service account, resources, probes, non-root and read-only security, Spot scheduling, PDB, and `ClusterIP`. |

### Compatibility patterns and exceptions

- The representative capabilities are content-oriented skills. Native CodeArts skill placement was sufficient; no OpenCode plugin or npm wrapper was needed first.
- Upstream source may live under `skills/.curated/...`, `skills/cloud/...`, or `pm-execution/skills/...`; CodeArts installation still uses `.codeartsdoer/skills/<skill-name>`. Upstream parent directories must not be copied into the native target structure.
- CodeArts CLI 26.8.1 uses YAML frontmatter for discovery but omits it from content returned by the `skill` tool. An initial Humanizer smoke test that asked about a frontmatter field was therefore invalid; a body-based capability assertion passed. Future assertions should come from the skill body.
- Humanizer's `skills/humanizer/SKILL.md` is a Git symlink to the repository-root `SKILL.md`. A default Windows checkout materializes it as a plain file containing only `../../SKILL.md`, so the guide copies the pinned root file and records `Adapter Required`.
- Scientific Experimental Design's Python scripts passed static review and syntax compilation, but `numpy`, `pandas`, and `pyDOE3` were not installed and CodeArts did not execute the scripts. The result is therefore `Partial`.
- Basic Google GKE manifest generation passed, but no live cluster, `gcloud`, Google Developer Knowledge API, or inference workload was tested. The result is therefore `Partial`.
- Two format checks initially rejected correct content: the Conventional Commit was wrapped in a code fence and an Obsidian single-character response was wrapped in backticks. The checker was corrected after reviewing full tool events and actual text; no compatibility claim rests on the format false negatives.
- The first user rollback used an incorrect relative working directory. Path-boundary checks rejected every move and no file was removed; the exact rollback then passed using resolved absolute paths.

### Scope limits

The environment remained Windows 11, PowerShell 7.6.5, CodeArts CLI 26.8.1, and MiMo `mimo/mimo-v2.5`. CodeArts desktop/IDE, Linux, other models, and unselected skills in the ten repositories were not tested. `Works` means only that the selected skill needed no additional adapter within this boundary.

---

## Native i-have-adhd skill verification

This section records a new candidate-discovery, pinned-source audit, and real CodeArts runtime pass performed around 12:00 on 2026-08-19 (Asia/Hong_Kong). Before verification, clean `main` had both local `HEAD` and `origin/main` at `9fbeda8a0edb7da1d9f2a3ecaeef913a73be6b7d`; only then did this section and its guide change.

### Candidate snapshot and selection

The snapshot combines GitHub Topics/Search with each repository's default-branch commit, Releases, and Issues API data from the same period. Stars and forks are selection signals, not quality or security guarantees.

| Candidate | Stars / Forks | Latest default-branch commit | Latest release | Maintenance, complexity, and risk | Disposition |
| --- | ---: | --- | --- | --- | --- |
| [wshobson/agents](https://github.com/wshobson/agents) | 38,909 / 4,143 | `367cb6a`, 2026-08-18 | None | Active, MIT; broad multi-agent and plugin marketplace audit surface | Later |
| [OthmanAdi/planning-with-files](https://github.com/OthmanAdi/planning-with-files) | 26,239 / 2,199 | `9b7d0a0`, 2026-08-14 | `v3.10.1` | Active, MIT; npm, hooks, and persistent-file workflow | Dedicated plugin pass later |
| [JimLiu/baoyu-skills](https://github.com/JimLiu/baoyu-skills) | 25,136 / 2,799 | `6b7a2e4`, 2026-07-04 | `v2.5.2` | MIT; broad skill set with external media/services in some workflows | Per-skill later |
| [alirezarezvani/claude-skills](https://github.com/alirezarezvani/claude-skills) | 24,634 / 3,471 | `aa8d778`, 2026-07-17 | `v2.9.0` | MIT; large skills, agents, and commands collection | Per-skill later |
| [ayghri/i-have-adhd](https://github.com/ayghri/i-have-adhd) | 22,028 / 1,398 | `e7555fc`, 2026-08-19 | None; manifest `0.2.0` | Active, MIT; small dependency-free core skill, with other plugin/hook paths separable | **Selected** |
| [Orchestra-Research/AI-Research-SKILLs](https://github.com/Orchestra-Research/AI-Research-SKILLs) | 11,818 / 862 | `773a529`, 2026-06-16 | `v1.7.2` | MIT; broader research toolchain and external dependencies | Per-skill later |
| [nidhinjs/prompt-master](https://github.com/nidhinjs/prompt-master) | 11,391 / 1,350 | `d15eabb`, 2026-06-10 | None | MIT; content-only but generic behavior needs a stronger attribution test | Later |
| [Jeffallan/claude-skills](https://github.com/Jeffallan/claude-skills) | 11,065 / 1,047 | `882ef55`, 2026-08-07 | `v0.4.16` | Active, MIT; 66 specialist skills requiring individual audit | Per-skill later |

`ayghri/i-have-adhd` was the strongest uncovered candidate after combining popularity, same-day maintenance, license, practical value, Windows portability, minimal install surface, and observable acceptance behavior. Its core value is shaping responses into action-first, numbered, low-tangent steps with one concrete next action. That makes it useful both as a pinned project skill and as a cross-project user tool.

### Pinned version and security review

- Pinned commit: `e7555fcaf612dfa1739dc86610ea926a906db614`. The repository has no GitHub release; root `package.json` reports version `0.2.0`.
- License: MIT.
- Installed file: `skills/i-have-adhd/SKILL.md`, 6,953 bytes, SHA-256 `13969C0AA5A69127828EF10FB4A53AA2B56D08C184B9F4354D0EA34695C358BB`.
- Root `package.json` has no dependencies, scripts, install, or postinstall. The repository has no lockfile and no binary required by this installation.
- The repository includes an OpenCode plugin, Claude hooks, PowerShell/Shell/Node scripts, and TypeScript extensions. Their manifests, file access, environment variables, network/credential/telemetry indicators were inspected. None was copied into CodeArts or executed.
- The upstream OpenCode dynamic skill path was not used. Following the compatibility skill's first decision tier, only the content-oriented `SKILL.md` was copied into native `.codeartsdoer/skills/i-have-adhd`.
- No npm installation occurred, so no lifecycle script needed to be enabled or disabled. Git only cloned the named repository and checked out the exact commit.

### Environment and official boundary

- Windows 11 Pro 10.0.26200 and PowerShell 7.6.5.
- CodeArts CLI 26.8.1, Node.js 24.14.1, npm 11.11.0, and Git 2.53.0.windows.2.
- Model: `mimo/mimo-v2.5`. Real invocations used `--format json --sandbox`, never `--auto`.
- The local CodeArts custom-provider preflight required `CODEARTS_CLI_AK` and `CODEARTS_CLI_SK` to exist. Tests supplied non-secret placeholders only for this preflight; the existing Provider configuration still supplied the real model credential. No credential was read, printed, or written to the repository.
- [Official CLI Skills documentation](https://support.huaweicloud.com/intl/en-us/usermanual-cli/codeartsagent_cli_0019.html) defines project `<project>/.codeartsdoer/skills` and user `~/.codeartsdoer/skills`, and says project scope wins a name collision.

### Final installation flow

The exact guide layout was re-run from zero before publication:

1. Project one cloned the pin into `.codeartsdoer/vendor/i-have-adhd` and copied only `skills/i-have-adhd/SKILL.md` to `.codeartsdoer/skills/i-have-adhd/SKILL.md`.
2. `codearts debug skill` resolved the project-one target, whose copied file matched the upstream SHA-256.
3. A second fresh project independently cloned the same commit, copied the same file, and resolved its own project target.
4. User scope cloned the pin into `~/.codeartsdoer/vendor/i-have-adhd` and copied only the same file to `~/.codeartsdoer/skills/i-have-adhd/SKILL.md`.
5. User invocation ran from an independent directory with no same-named project skill. User-root `package.json` had the same hash before and after installation; `codearts_cli.json` and credentials were untouched.

### Discovery, loading, and core invocation

The final smoke prompt allowed one `i-have-adhd` call only, then exercised three rules chosen from the skill body: make the first action open the text, use exactly three steps, and finish by circling the first `Error` line within two minutes.

| Scope | Completed skill event | Base directory | Attributable result |
| --- | --- | --- | --- |
| Project one | `call_62d255726b314865876c91e9`, `skill`, `completed` | Project-one `.codeartsdoer/skills/i-have-adhd` | Three numbered actions; first opened the error text and last circled the first `Error` within two minutes |
| Project two | `call_507049ffea574baf854fb616`, `skill`, `completed` | Project-two `.codeartsdoer/skills/i-have-adhd` | Same structure and terminal action |
| User | `call_29c2652ba0084b7f903fa65d`, `skill`, `completed` | `C:/Users/AI_BA/.codeartsdoer/skills/i-have-adhd` | Same structure, with no same-named project override |

All three final-flow runs exited 0 and emitted only one `skill` tool event, with no Bash, file, time, or other tool event. Acceptance assertions came from the `SKILL.md` body rather than YAML frontmatter that CodeArts' runtime tool does not return.

### Counterevidence and limits

- The first exploratory user-scope smoke call completed the intended skill but then made an extra sandbox Bash call to read system time, violating the no-other-tool contract. It did not count as final acceptance. A stricter self-contained prompt that explicitly treated any second tool as failure then passed. Tool restrictions remain model-compliance dependent.
- With project and user copies present simultaneously, CodeArts CLI 26.8.1 twice resolved the user path in `codearts debug skill`. Removing the user copy immediately restored the project path. This contradicts the current official CLI documentation's project-first rule. The guide therefore requires selecting one scope and stopping on a same-name collision.
- Only one explicit invocation was verified. Upstream frontmatter `disable-model-invocation`, session persistence, `stop adhd mode`, always-on hooks, OpenCode/Claude/Codex plugins, and auxiliary `agents` metadata were not tested.
- CodeArts IDE, Linux, other models, and any medical or accessibility outcome were not tested.

### Rollback

- Before deletion, six exact absolute targets were individually resolved and checked: the skill and vendor in each of two projects, plus the user skill and vendor. No broad `.codeartsdoer` or home-directory deletion was used.
- After the final rollback, `codearts debug skill` returned zero `i-have-adhd` matches from project one, project two, and the independent user consumer.
- A separate real post-removal CodeArts session reported that `i-have-adhd` was unavailable and emitted no skill tool event.
- User-root `package.json` SHA-256 returned to the pre-install baseline. User skill/vendor were absent; `codearts_cli.json`, credentials, and unrelated skills were unchanged.

All five gates—real installation, CodeArts discovery/loading, core invocation, second-project reproduction, and exact rollback—passed. The result is **Works**. See the formal [中文 guide](../guides/i-have-adhd/README.md) · [English](../guides/i-have-adhd/README.en.md).

### Sources

- [Pinned commit](https://github.com/ayghri/i-have-adhd/tree/e7555fcaf612dfa1739dc86610ea926a906db614)
- [Pinned SKILL.md](https://github.com/ayghri/i-have-adhd/blob/e7555fcaf612dfa1739dc86610ea926a906db614/skills/i-have-adhd/SKILL.md)
- [MIT License](https://github.com/ayghri/i-have-adhd/blob/e7555fcaf612dfa1739dc86610ea926a906db614/LICENSE)
- [CodeArts CLI Skills](https://support.huaweicloud.com/intl/en-us/usermanual-cli/codeartsagent_cli_0019.html)
- [CodeArts CLI commands](https://support.huaweicloud.com/intl/en-us/usermanual-cli/codeartsagent_cli_0034.html)
