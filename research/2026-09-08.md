# CodeArts 兼容验证记录 — 2026-09-08

[English](2026-09-08.en.md)

## 批次结论

本批实际推进 10 个此前未覆盖的 `phuryn/pm-skills` Skill。每项都在真实 CodeArts CLI 中独立完成项目 A、全新项目 B、个人级安装、发现、调用与精确回滚。机器门槛初筛有两项 3/3 通过；人工语义审计撤回 `positioning-ideas`，因为三份输出均把输入之外的能力或竞品特征写成事实。最终只有 `product-name` 达到正式指南门槛并发布中英文指南，其余九项只保留研究结论。

| 候选 | 评分 | 安装/发现 | 加载 | 核心调用 | 第二环境复现 | 回滚 | 结论 |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| `monetization-strategy` | 24/25 | 3/3 | 3/3 | 2/3 | 是 | 3/3 | Partial：个人级缺核心结构 |
| `strategy-red-team` | 24/25 | 3/3 | 3/3 | 0/3 | 否 | 3/3 | Partial：标记缺失，项目 B 超时 |
| `user-segmentation` | 24/25 | 3/3 | 3/3 | 2/3 | 否 | 3/3 | Partial：项目 B 额外使用 `todowrite` |
| `positioning-ideas` | 23/25 | 3/3 | 3/3 | 机读 3/3；语义 0/3 | 是 | 3/3 | Partial：虚构能力与竞品事实 |
| `test-scenarios` | 24/25 | 3/3 | 3/3 | 2/3 | 是 | 3/3 | Partial：个人级超时并写入失败 |
| `product-name` | 24/25 | 3/3 | 3/3 | 3/3 | 是 | 3/3 | **Works** |
| `grammar-check` | 24/25 | 3/3 | 3/3 | 2/3 | 是 | 3/3 | Partial：个人级超时 |
| `sentiment-analysis` | 24/25 | 3/3 | 3/3 | 2/3 | 否 | 3/3 | Partial：项目 B 超时 |
| `review-resume` | 23/25 | 3/3 | 3/3 | 1/3 | 否 | 3/3 | Partial：额外 Bash 与项目 B 超时 |
| `wwas` | 24/25 | 3/3 | 2/3 | 2/3 | 否 | 3/3 | Partial：项目 A 双 Skill 事件 |

评分满分 25：维护活跃度、许可证清晰度、实用性、安装简洁度、Windows/安全风险各 5 分。评分只用于候选排序，不代替真实运行门槛。

## 来源与固定版本

- 选择日期：2026-09-08。候选来自 [GitHub Agent Skills Topic](https://github.com/topics/agent-skills)、[PM Skills 上游仓库](https://github.com/phuryn/pm-skills)、发布、提交、Issues、Pull Requests 与仓库文档。
- GitHub CLI 当日快照：26,095 stars、2,801 forks；仓库最后推送时间为 2026-07-03，最新发布为 v2.1.0。
- `git ls-remote` 确认默认分支 `main` 与 `v2.1.0` 都指向 [`18468a95b427e70e258b51389796367c6f684e7d`](https://github.com/phuryn/pm-skills/commit/18468a95b427e70e258b51389796367c6f684e7d)。
- 仓库许可证为 MIT；固定 `LICENSE` 的 SHA-256 为 `591AB4C6A9E75F1F2CF517522759F96EB19F928EE72E637F32FD7D1337C533DF`。
- CodeArts 官方 Skills 文档于 2026-09-02 更新，仍规定项目级位置为 `<项目>/.codeartsdoer/skills`、个人级位置为 `~/.codeartsdoer/skills`，同名优先级为项目级高于个人级。

## 环境与隔离

- CodeArts CLI 26.8.1：`%USERPROFILE%\.codeartsdoer\installers\codearts.cmd`。
- Windows 11 build 26200；PowerShell 7.6.5；Git 2.53.0.windows.2；Node 24.14.1；npm 11.11.0。
- 模型：`mimo/mimo-v2.5`。自动化进程没有继承 `CODEARTS_CLI_AK` 或 `CODEARTS_CLI_SK`；验证子进程只设置未持久化、非秘密的占位值以通过 CodeArts 自定义 Provider 前置检查，实际模型鉴权仍使用已有 Provider 配置。未打印、复制、哈希或修改任何密钥。
- 项目 A/B 位于 `.tmp/verification/2026-09-08-pm5/runs/<name>-a` 与 `<name>-b`；个人级 consumer 为 `<name>-user`，没有同名项目级覆盖。
- 项目级目标为 `<项目>/.codeartsdoer/skills/<name>` 与 `<项目>/.codeartsdoer/vendor/pm-skills-<name>`；个人级目标为 `~/.codeartsdoer/skills/<name>` 与 `~/.codeartsdoer/vendor/pm-skills-<name>`。
- 每个范围都从固定上游建立独立 sparse checkout，只复制候选目录；先运行 `codearts debug skill`，再运行候选专用 `codearts run -m mimo/mimo-v2.5 --format json`。单次调用上限 90 秒，超时不重试。

## 安全审查

10 个目标目录各自只包含一个 Git 模式为 `100644` 的普通文本 `SKILL.md`。没有 manifest、锁文件、install/postinstall、脚本、二进制、下载器、MCP、Hook、Command、遥测或凭据读取。正文只包含行为说明和延伸阅读链接；代表测试明确禁止网络与文件写入。

安装前确认 10 个个人级 Skill 与 vendor 目标均不存在。安装不运行上游 `validate_plugins.py`、测试或其他插件内容，不使用 `--auto`，也不修改用户根 `package.json`、`codearts_cli.json` 或凭据。

## 各候选证据

范围顺序均为项目 A / 项目 B / 个人级。

### `monetization-strategy` — Partial

- 三范围都被精确发现，并各有一次来自预期路径的完成事件：`call_ed57d88c1d9049b2befc9622`、`call_be32ea85d8b845c6893bbaf9`、`call_8fe733cd1e18414d907c6f13`。
- 两个项目范围完成四种变现策略、风险、隐私约束、实验与阈值；个人级最终文本缺少 `PRODUCT`、四个 `STRATEGY-*` 与 `COMPARISON` 结构，因此核心调用只有 2/3。
- 3/3 精确回滚通过。

### `strategy-red-team` — Partial

- 3/3 精确发现和目标路径加载。项目 A `call_dfe307a6598b4f42ac807700` 缺 `CHEAPEST-TEST`；个人级 `call_45d2d3097c624feebe39f4ea` 缺多项指定结构。
- 项目 B 在 Skill `call_1305598cad36462bba03b108` 后额外运行三次 `todowrite` 并于 90 秒超时，无最终文本。核心调用 0/3；3/3 回滚通过。

### `user-segmentation` — Partial

- 三范围目标 Skill 分别为 `call_e238c14f40ed402d8b5254f5`、`call_d982f3bd87c440a4abcb388f`、`call_d2e9d1dcd6dc48edaa682130`，均精确加载。
- 项目 A 与个人级完成三分群和 U1-U9 唯一映射；项目 B 虽通过文本断言，却额外完成三次 `todowrite`，违反只调用一次 Skill 的固定流程。核心调用 2/3；3/3 回滚通过。

### `positioning-ideas` — Partial

- 机读门槛初始 3/3 通过，只有目标 Skill 事件：`call_3bdee9de7ee7479fbdee7735`、`call_0fe03d2a524c4dfc8d6f805a`、`call_80e9f00a62784a769bfcbee2`。
- 人工语义审计撤回通过。项目 A无依据声称通用日历常有广告、依赖网络和损害隐私；项目 B声称同步默认关闭、端到端加密和“小团队响应快”；个人级还声称境外服务器、无投资人数据压力、自动排序等输入未提供能力。
- 这些都是定位承诺中的实质事实，不能用结尾的 `NO-INVENTED-CLAIMS` 抵消。3/3 回滚通过，不发布指南。

### `test-scenarios` — Partial

- 项目 A/B 的 `call_184b895c92fd4011a769ce6c`、`call_91353476cf99456c8bac59c1` 完成六个场景、可观察结果与追踪关系。
- 个人级在目标 Skill `call_ea495419990d415eb9ca1743` 后额外调用 Bash，并出现 `write:error`，随后超时且无最终文本。核心调用 2/3；3/3 回滚通过。

### `product-name` — Works

- 三范围分别由 `call_1f23fde5717c456c910c80a9`、`call_21ef33444ce14ce699614994`、`call_f2238af3d95945569b735003` 完成唯一目标 Skill；事件输出的 base directory 精确解析到所选项目或个人级原生目录。
- 三份结果都生成恰好五个名称；禁用词未进入名称；每项都有理由、品牌匹配、记忆度、发音、风险和 `UNCHECKED` 域名/商标状态，并包含排序与人工验证建议。没有网络、写入或其他工具事件。
- 3/3 安装、发现、加载、核心调用和回滚全部通过，发布 [中文指南](../guides/pm-product-name/README.md) 与 [English guide](../guides/pm-product-name/README.en.md)。

### `grammar-check` — Partial

- 项目 A/B 调用 `call_01c6c52add67450d86d8ff03`、`call_6e4fc092225c4f0ba3bc985f` 完成语法、逻辑、流畅性、修复建议和优先级。
- 个人级完成目标 Skill 后执行三次 `todowrite`，在 90 秒内没有最终文本。核心调用 2/3；3/3 回滚通过。

### `sentiment-analysis` — Partial

- 项目 A `call_d607507d32614d48a7bb53e9` 与个人级 `call_30207da3148347cf8fda97c4` 完成三分群、记录映射、比例、情感分数和行动。
- 项目 B 完成目标 Skill 后额外运行两次 `todowrite`，随后超时，无最终文本。核心调用 2/3；3/3 回滚通过。

### `review-resume` — Partial

- 项目 A 的目标 Skill 为 `call_b2c706f7b48646db8c062de6`，文本覆盖 10 项检查，但又完成两次不必要 Bash 调用；项目 B `call_26317207190c4e62a3d01ebe` 超时且无最终文本。
- 个人级 `call_077f57dcacbf4da68d138c69` 独立完成。核心调用 1/3；3/3 回滚通过。

### `wwas` — Partial

- 项目 B `call_49455c8e3050425f9c534fa2` 与个人级 `call_712ea108728d4e9fb31ace9e` 完成三个 Why-What-Acceptance 条目。
- 项目 A 连续完成两个同名 Skill 事件（`call_c4522f4e91cb49d1bb5a9b53`、`call_054a05b5b4de43ca84d2ca5f`），并缺少 `ONE-SPRINT`，因此目标加载与核心流程不稳定。3/3 回滚通过。

## 回滚与基线

- 30/30 次卸载后都再次运行真实 `codearts debug skill`；旧绝对路径全部消失，所有项目与个人级 Skill/vendor 目标均不存在。
- 用户根 `package.json` 前后均为 SHA-256 `589A2F93D6B401FCCE4EDED58C4C477C3891C3B580796862C58ACB4E5D73A4E7`。
- 用户根 `codearts_cli.json` 前后均为 SHA-256 `6F08A23430F657E3CBF75ABDCF1CB7A08FE88504F6FDC51BA31B6530386E2E23`。
- `.tmp` 只保留忽略的审计证据，不纳入 Git 提交。

## 发布边界

本批只为 `product-name` 新增正式指南和根索引。其余九项只能说明固定内容可被 CodeArts 原生安装和发现，或在部分范围可调用；不能宣称跨范围稳定 Works。

结论只覆盖 Windows 11、CodeArts CLI 26.8.1、`mimo/mimo-v2.5`、MIT v2.1.0 固定提交与本页列出的离线代表流程。未验证 Linux、macOS、其他模型、联网研究、文件保存、Claude Commands 或完整插件编排。

## 来源

- [CodeArts CLI Skills 文档（中文，2026-09-02 更新）](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0019.html)
- [CodeArts CLI 命令文档](https://support.huaweicloud.com/intl/en-us/usermanual-cli/codeartsagent_cli_0034.html)
- [PM Skills 固定提交](https://github.com/phuryn/pm-skills/tree/18468a95b427e70e258b51389796367c6f684e7d)
- [PM Skills v2.1.0](https://github.com/phuryn/pm-skills/releases/tag/v2.1.0)
- [PM Skills Issues](https://github.com/phuryn/pm-skills/issues) 与 [Pull Requests](https://github.com/phuryn/pm-skills/pulls)
- [GitHub Agent Skills Topic](https://github.com/topics/agent-skills)
