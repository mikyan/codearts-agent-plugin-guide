# CodeArts 兼容验证记录 — 2026-08-19

[English](2026-08-19.en.md)

本记录补充 [2026-08-18 项目级验证](2026-08-18.md)，重点验证 Ponytail 和 Superpowers 能否安装到当前用户的 CodeArts 目录，并在没有项目级配置的其他项目中使用。

## 结论

| 项目 | 固定版本 | 项目级 | 个人级 | 兼容状态 |
| --- | --- | --- | --- | --- |
| [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) | 4.9.0，`0a4dd63ad4541f4f655c4108a295916f3c1d8fda` | 已验证 | 已验证 | Adapter Required |
| [obra/superpowers](https://github.com/obra/superpowers) | 6.3.0，`b36e0829c6d0140e93cfef2ca599b1b07d4a7797` | 已验证 | 已验证 | Adapter Required |

CodeArts 官方文档支持项目级 `.codeartsdoer/skills` 与个人级 `~/.codeartsdoer/skills`，同名时项目级优先；插件也分别支持项目和个人目录。个人级在这里表示“当前用户的所有 CodeArts 项目可用”，不是整台机器所有用户共享。

## 验证环境

- Windows 11 Build 26200
- CodeArts CLI 26.8.1
- PowerShell 7.6.5
- Node.js 24.14.1、npm 11.11.0、Git 2.53.0
- 自定义模型：`mimo/mimo-v2.5`
- 凭据只由 CodeArts 进程继承，未输出或写入仓库
- 真实调用使用 `codearts run --format json --sandbox`，未使用 `--auto`

本轮没有验证 CodeArts 桌面端/IDE 和 Linux。

## 发现的问题

测试前，用户根 `%USERPROFILE%/.codeartsdoer/package.json` 已包含 CodeArts 自带的 `@opencode-ai/plugin@26.8.1`。如果直接把第三方依赖合并进这个文件，再在用户根安装，系统 npm 和 CodeArts 自动 Reify 都会尝试从公共 npm Registry 解析该内部版本并返回 `ETARGET`。

因此个人级适配不能直接复用项目级依赖清单。验证通过的结构是：

```text
~/.codeartsdoer/
  plugins/<project>.ts
  vendor/<project>/
    package.json
    package-lock.json
    node_modules/
  skills/<copied-skill>/
```

每个项目拥有独立的 `vendor` 清单。插件使用相对于 `~/.codeartsdoer/plugins` 的固定路径加载上游入口，Skills 则复制到 CodeArts 原生个人级目录。用户根 `package.json` 保持不变。

## Ponytail 实测

- `vendor/ponytail/package.json` 只固定 `@dietrichgebert/ponytail` 4.9.0。
- 使用 `npm install --ignore-scripts --no-audit --no-fund` 安装。
- `plugins/ponytail.ts` 加载上游 `.opencode/plugins/ponytail.mjs`。
- 6 个 Skill 目录复制到个人级 `skills`。
- 从一个没有项目 `.codeartsdoer` 的独立目录运行 CodeArts。
- `ponytail-help` 产生成功的原生 `skill` 工具事件，其基础目录位于个人级 Skills 路径，最终输出为：

```text
Lite, Full, Ultra
```

## Superpowers 实测

- `vendor/superpowers/package.json` 固定 Git Tag `v6.3.0`。
- 使用 `npm install --ignore-scripts --no-audit --no-fund` 安装。
- `plugins/superpowers.ts` 加载上游 `.opencode/plugins/superpowers.js`。
- 14 个 Skill 目录复制到个人级 `skills`。
- 从同一个无项目级配置的独立目录运行 CodeArts。
- `systematic-debugging` 产生成功的原生 `skill` 工具事件，其基础目录位于个人级 Skills 路径，最终输出为：

```text
NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST
```

## 回滚验证

Ponytail 和 Superpowers 的个人级插件、独立 `vendor` 目录与对应 Skill 目录均被完整移出用户 CodeArts 目录。随后确认：

- 两组第三方 Skills 不再被发现；
- 用户根 `package.json` 的 SHA-256 与测试前完全相同；
- `codearts_cli.json`、凭据和其他 CodeArts 插件没有被修改。

项目级安装此前已在两个隔离项目中分别复现并完成回滚。两个范围的安装与卸载步骤已经写入正式指南。

## 已发布指南

- [Ponytail — 中文](../guides/ponytail/README.md) · [English](../guides/ponytail/README.en.md)
- [Superpowers — 中文](../guides/superpowers/README.md) · [English](../guides/superpowers/README.en.md)

## 第一方来源

- [CodeArts CLI Skills](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0019.html)
- [CodeArts CLI Hooks](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0018.html)

---

## 十个热门 Agent Skills 项目批量验证

在 Ponytail 与 Superpowers 的适配经验基础上，本轮按 2026-08-19 的 GitHub Star 快照选择十个热门项目。每个仓库只选择一个代表性 Skill；结论不外推到仓库中的其他 Skills、插件或客户端。

| 上游项目 | Star 快照 | 固定 Commit | 本次 Skill | 结论 | 指南 |
| --- | ---: | --- | --- | --- | --- |
| [anthropics/skills](https://github.com/anthropics/skills) | 170,227 | `0a64e39` | `frontend-design` | Works | [中文](../guides/anthropic-skills/README.md) · [English](../guides/anthropic-skills/README.en.md) |
| [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills) | 88,268 | `df1edb2` | `code-simplification` | Works | [中文](../guides/addyosmani-agent-skills/README.md) · [English](../guides/addyosmani-agent-skills/README.en.md) |
| [kepano/obsidian-skills](https://github.com/kepano/obsidian-skills) | 46,655 | `a1dc48e` | `obsidian-markdown` | Works | [中文](../guides/obsidian-skills/README.md) · [English](../guides/obsidian-skills/README.en.md) |
| [github/awesome-copilot](https://github.com/github/awesome-copilot) | 37,982 | `318066d` | `commit-message-storyteller` | Works | [中文](../guides/github-awesome-copilot/README.md) · [English](../guides/github-awesome-copilot/README.en.md) |
| [blader/humanizer](https://github.com/blader/humanizer) | 36,342 | `ebf637b` | `humanizer` | Adapter Required | [中文](../guides/humanizer/README.md) · [English](../guides/humanizer/README.en.md) |
| [K-Dense-AI/scientific-agent-skills](https://github.com/K-Dense-AI/scientific-agent-skills) | 33,819 | `9e8b0cb` | `experimental-design` | Partial | [中文](../guides/scientific-agent-skills/README.md) · [English](../guides/scientific-agent-skills/README.en.md) |
| [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills) | 30,164 | `b8caa26` | `vercel-react-best-practices` | Works | [中文](../guides/vercel-agent-skills/README.md) · [English](../guides/vercel-agent-skills/README.en.md) |
| [phuryn/pm-skills](https://github.com/phuryn/pm-skills) | 25,391 | `18468a9` | `prioritization-frameworks` | Works | [中文](../guides/pm-skills/README.md) · [English](../guides/pm-skills/README.en.md) |
| [openai/skills](https://github.com/openai/skills) | 25,018 | `49f948f` | `security-best-practices` | Works | [中文](../guides/openai-skills/README.md) · [English](../guides/openai-skills/README.en.md) |
| [google/skills](https://github.com/google/skills) | 18,462 | `65ef106` | `gke-manifest-generation` | Partial | [中文](../guides/google-skills/README.md) · [English](../guides/google-skills/README.en.md) |

Star 只是本轮选择候选的热度快照，不是质量或安全保证。所有安装和文档都固定到表中 Commit，不跟随浮动分支。

### 复用的验证方法

本轮实际使用仓库内的 [CodeArts 插件兼容验证 Skill](../skills/codearts-plugin-compatibility/SKILL.md) 执行以下流程：

1. 审查固定 Commit 下的 `SKILL.md`、本地资源、许可证与可执行脚本。
2. 只把所选 Skill 复制到隔离项目的 `.codeartsdoer/skills/<skill-name>`，不执行上游安装脚本、不安装无关依赖。
3. 用 `codearts debug skill` 检查名称和来源路径，再用真实 `codearts run --format json --sandbox --model mimo/mimo-v2.5` 要求模型显式调用该 Skill。
4. 在第二个全新隔离项目从零复现同一流程。
5. 把同一组 Skills 安装到当前用户 `~/.codeartsdoer/skills/<skill-name>`，从没有项目级覆盖的独立目录再次调用。
6. 精确移除测试目标，确认项目级与个人级均不再发现这些 Skills，并确认用户根 `package.json` 哈希没有变化。

十个项目都完成了“发现 + 成功的 `skill` 工具事件 + 可归因结果”；OpenAI 的用例还完成了一次 Skill 内相对路径 `read`。测试过程中没有把 AK、SK 或模型 API Key 写入命令输出和仓库。自定义模型会话使用非秘密 AK/SK 占位值，真实模型凭据仍由本地 `codearts_cli.json` 中的 Provider 配置使用。

### 真实任务结果

| Skill | 可归因的验收结果 |
| --- | --- |
| `frontend-design` | 为泡茶计时器给出四个指定设计维度，并提供至少四个六位十六进制颜色。 |
| `code-simplification` | 把分支式 JavaScript 校验函数简化为直接返回布尔表达式。 |
| `obsidian-markdown` | 生成含 YAML Frontmatter、`[[Roadmap]]` 内链和 Tip Callout 的 Obsidian 笔记。 |
| `commit-message-storyteller` | 根据缓存复用改动生成以 `perf(cache)` 开头、解释重复工作和延迟的 Conventional Commit。 |
| `humanizer` | 保留事实年份 `2024`，移除指定的夸张和套话表达。 |
| `experimental-design` | 正确识别独立样本数为 4、实验单位为 mouse，而不是把 80 个细胞当成独立样本。 |
| `vercel-react-best-practices` | 用 `Promise.all` 消除两个互不依赖的顺序 `await`，同时保持返回结构。 |
| `prioritization-frameworks` | 正确计算 RICE 分数 `25`。 |
| `security-best-practices` | Skill 调用后读取其 Express 安全参考文件，并返回 `helmet()`。 |
| `gke-manifest-generation` | 生成含命名空间、ServiceAccount、资源限制、探针、非 Root、只读根文件系统、Spot 调度、PDB 和 `ClusterIP` 的 YAML。 |

### 兼容规律与例外

- 这十个候选的代表性能力都是内容型 Skill。优先采用 CodeArts 原生 Skills 目录即可，无需先构造 OpenCode 插件或 npm 包装层。
- 上游源码可以位于 `skills/.curated/...`、`skills/cloud/...` 或 `pm-execution/skills/...` 等深层目录；安装到 CodeArts 时仍统一落到 `.codeartsdoer/skills/<skill-name>`。不要照抄上游父目录层级。
- CodeArts CLI 26.8.1 的 `skill` 工具返回正文但不返回 YAML Frontmatter。最初用 Frontmatter 字段做验收的 Humanizer 冒烟测试因此无效，改用正文能力后通过。后续验收应从 Skill 正文选择可观察断言。
- Humanizer 的 `skills/humanizer/SKILL.md` 是指向仓库根 `SKILL.md` 的 Git 符号链接。Windows 默认检出会把它变成只含 `../../SKILL.md` 的普通文件，因此指南复制固定 Commit 的仓库根文件，结论为 `Adapter Required`。
- Scientific Experimental Design 随附的 Python 脚本已完成静态审查和语法编译，但没有安装 `numpy`、`pandas`、`pyDOE3`，也没有让 CodeArts 执行脚本，因此只记为 `Partial`。
- Google GKE Skill 的基本清单生成通过，但未连接真实 GKE 集群、运行 `gcloud`、访问 Google Developer Knowledge API 或验证推理工作负载，因此只记为 `Partial`。
- 两次输出格式检查曾把正确结果误判为失败：Conventional Commit 被代码围栏包裹，Obsidian 单字符结果被反引号包裹。复核完整工具事件与实际文本后修正了测试器；兼容结论没有建立在格式误判上。
- 第一次个人级回滚使用了错误的相对工作目录，路径边界检查拒绝了全部移动，没有文件被误删；改用已解析的绝对路径后完成精确回滚。

### 范围限制

本轮验证环境仍为 Windows 11、PowerShell 7.6.5、CodeArts CLI 26.8.1 和 MiMo `mimo/mimo-v2.5`。未验证 CodeArts 桌面端/IDE、Linux、其他模型，以及十个上游仓库中没有列入表格的其他 Skills。`Works` 只表示所选 Skill 在上述边界内无需额外适配即可使用。
