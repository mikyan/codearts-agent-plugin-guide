# CodeArts 兼容验证记录 — 2026-09-07

[English](2026-09-07.en.md)

## 批次结论

本批实际推进 10 个此前未覆盖的 `phuryn/pm-skills` Skill。每项都在真实 CodeArts CLI 中独立完成项目 A、全新项目 B、个人级安装与回滚。首轮只有 `brainstorm-ideas-existing` 通过全部机读门槛，但把“未提供事实必须标成假设”写进最终教程提示词后，额外三范围发布流复验未能复现；因此 10 项最终全部为 Partial，本批只新增中英文研究记录，不创建正式指南。

| 候选 | 评分 | 安装 | 加载 | 核心调用 | 第二环境复现 | 回滚 | 结论 |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| `brainstorm-experiments-existing` | 23/25 | 3/3 | 2/3 | 2/3 | 是 | 3/3 | Partial：个人级 TLS 证书错误 |
| `brainstorm-experiments-new` | 23/25 | 3/3 | 3/3 | 1/3 | 否 | 3/3 | Partial：项目 A 与个人级超时 |
| `brainstorm-ideas-existing` | 24/25 | 3/3 | 3/3 | 初测 3/3；发布流 1/3 | 否 | 3/3 | Partial：收紧后的教程流程未复现 |
| `brainstorm-okrs` | 24/25 | 3/3 | 3/3 | 1/3 | 否 | 3/3 | Partial：项目 A 漏标记，项目 B 超时 |
| `gtm-motions` | 23/25 | 3/3 | 3/3 | 2/3 | 否 | 3/3 | Partial：项目 A 超时 |
| `gtm-strategy` | 23/25 | 3/3 | 2/3 | 1/3 | 否 | 3/3 | Partial：项目 A 双事件后超时，个人级超时 |
| `identify-assumptions-existing` | 24/25 | 3/3 | 2/3 | 1/3 | 否 | 3/3 | Partial：两个项目范围超时 |
| `identify-assumptions-new` | 23/25 | 3/3 | 3/3 | 0/3 | 否 | 3/3 | Partial：两个项目超时，个人级漏视角标记 |
| `job-stories` | 24/25 | 3/3 | 3/3 | 1/3 | 否 | 3/3 | Partial：项目 B 与个人级写入被拒绝 |
| `marketing-ideas` | 23/25 | 3/3 | 3/3 | 0/3 | 否 | 3/3 | Partial：结构通过但语义审计失败 |

评分满分 25：维护活跃度、许可证清晰度、实用性、安装简洁度、Windows/安全风险各 5 分。评分只用于候选排序，不代替真实运行门槛。

## 来源与固定版本

- 选择日期：2026-09-07。候选来自 [GitHub Agent Skills Topic](https://github.com/topics/agent-skills)、[PM Skills 上游仓库](https://github.com/phuryn/pm-skills)、发布、提交、Issues、Pull Requests 与仓库文档。
- GitHub 当日页面显示约 26.1k stars、2.8k forks、26 个 open issues、16 个 open pull requests 和 62 次提交。
- 上游默认分支 HEAD 与最新发布都固定为 [`18468a95b427e70e258b51389796367c6f684e7d`](https://github.com/phuryn/pm-skills/commit/18468a95b427e70e258b51389796367c6f684e7d)，标签 [`v2.1.0`](https://github.com/phuryn/pm-skills/releases/tag/v2.1.0)。`git ls-remote` 再次确认分支与标签指向相同提交。
- 仓库许可证为 MIT；本地固定 `LICENSE` 的 SHA-256 为 `591AB4C6A9E75F1F2CF517522759F96EB19F928EE72E637F32FD7D1337C533DF`。
- CodeArts 官方 Skills 文档于 2026-08-28 更新，仍规定项目级位置为 `<项目>/.codeartsdoer/skills`、个人级位置为 `~/.codeartsdoer/skills`，同名优先级为项目级高于个人级。

## 环境与隔离

- CodeArts CLI 26.8.1：`%USERPROFILE%\.codeartsdoer\installers\codearts.cmd`。
- Windows 11 build 26200；PowerShell 7.6.5；Git 2.53.0.windows.2；Node 24.14.1；npm 11.11.0。
- 模型：`mimo/mimo-v2.5`。当前自动化进程没有继承 `CODEARTS_CLI_AK` 或 `CODEARTS_CLI_SK`；验证子进程只设置未持久化、非秘密的占位值以通过 CodeArts 自定义 Provider 前置检查，实际模型鉴权仍使用已有 Provider 配置。未打印、复制、哈希或修改任何密钥。
- 项目 A 与 B 分别位于 `.tmp/verification/2026-09-07-pm4/runs/<name>-a` 和 `<name>-b`。个人级 consumer 为 `<name>-user`，且没有同名项目级覆盖。
- 项目级目标为 `<项目>/.codeartsdoer/skills/<name>` 与 `<项目>/.codeartsdoer/vendor/pm-skills-<name>`；个人级目标为 `~/.codeartsdoer/skills/<name>` 与 `~/.codeartsdoer/vendor/pm-skills-<name>`。
- 每个范围都从固定上游共享 clone 建立独立 sparse checkout，只复制候选目录；先运行 `codearts debug skill`，再运行候选专用 `codearts run -m mimo/mimo-v2.5 --format json`。单次调用上限 90 秒，超时不重试。

## 安全审查

10 个目标目录都只包含一个 Git 模式为 `100644` 的普通文本 `SKILL.md`。没有 manifest、锁文件、install/postinstall、脚本、二进制、下载器、MCP、Hook、Command、遥测或凭据读取。正文只包含行为说明和延伸阅读链接；代表测试明确禁止网络与文件写入。

安装前确认 10 个个人级 Skill 与 vendor 目标均不存在。安装不运行上游 `validate_plugins.py`、测试或其他插件内容，不使用 `--auto`，也不修改用户根 `package.json`、`codearts_cli.json` 或凭据。

## 各候选证据

范围顺序均为项目 A / 项目 B / 个人级。

### `brainstorm-experiments-existing` — Partial

- 三范围均安装并被 `codearts debug skill` 精确发现。项目 A/B 分别用调用 `call_6ff165ec22e44c8bb0980c26`、`call_9c2c910c813f48cba3b9aa33` 完成唯一目标 Skill，并输出 5 个假设对应的实验、指标、阈值、风险缓解和决策。
- 个人级 `codearts run` 在 Skill 事件前退出 1，原始错误为 `unknown certificate verification error`，没有最终文本或 call ID。因此个人级加载与核心调用未通过；没有重试。
- 三范围卸载与移除后诊断均通过。

### `brainstorm-experiments-new` — Partial

- 3/3 精确发现与加载。项目 B 调用 `call_e8e0592269c3465e801474f2` 完整输出 XYZ hypothesis、3 个 pretotype、YODA、阈值和决策规则。
- 项目 A `call_d7d61bdc94854497be96ae2b` 与个人级 `call_b13159d2165f41fb8d46251e` 在完成 Skill 后触及 90 秒上限，没有最终文本，不能复现代表能力。
- 3/3 回滚通过。

### `brainstorm-ideas-existing` — Partial

- 三范围调用 `call_ee39280a193a4d54a02d9462`、`call_c128e1dc3e364f94b066398b`、`call_de6512d607ce467faeb18a93` 都只有一次完成的目标 Skill 事件，来源路径分别解析到预期原生目录。
- 三次都输出 PM、Designer、Engineer 各 5 个创意，从 15 个创意中选出恰好 5 个优先项，并给出名称、描述、选择理由和待验证假设；无网络、写入或其他工具事件。
- 语义抽查保留一项限制：项目 B 把通勤时学习与网络状况写成“洞察”，这是输入之外的推断。创意主体仍把未知条件列为假设，因此首轮代表流程通过；拟发布提示词随后把所有未提供信息明确要求为假设，并要求人复核排序。
- 为让正式教程把限制变成可执行要求，发布流在同一提示词加入“所有未提供事实必须标成假设，不得把推断的用户行为或市场条件写成事实”，随后重新执行项目 A/B/个人级。项目 A 在目标 Skill 后额外完成两个 `todowrite` 事件并超时（`call_cbb9b2a11c014a15b296b74c`、`call_700df531f40f4a388ddbe40b`、`call_b8fc6f4c617c4231ab3fb0d8`）；项目 B 在目标 Skill 后违反明确禁令，额外用 `bash` 执行占位 `echo`（`call_9646a6d196f446deaf6176bd`、`call_cc9f6d01727c482fae6c909b`）；只有个人级 `call_4465ea8dc5074b8c8f289975` 完整通过。
- 发布流 3/3 仍精确发现与加载、3/3 回滚且用户基线哈希不变，但代表能力只有 1/3，不能发布正式指南。草拟指南与根索引已撤销。

### `brainstorm-okrs` — Partial

- 3/3 精确加载。个人级 `call_21521753540343e98168d09b` 完整通过。
- 项目 A `call_8619a2c7acbe4365b7f4abaf` 产生三套 OKR、基线列、健康指标和 OKR/KPI/NSM 关系，但漏掉要求的字面 `BASELINES` 标记；项目 B `call_945193b266774bc6aa152713` 在完成 Skill 后超时。
- 项目 B 还引用未提供的竞品参考数据，语义审计不接受为验证事实。3/3 回滚通过。

### `gtm-motions` — Partial

- 3/3 精确加载。项目 B `call_e6b51cd576b44be3891b518a` 与个人级 `call_b6e88756627b465fa063f53c` 完成 7 种 motion 评分、组合、预算、90 天计划和停止阈值。
- 项目 A `call_a290a32ee4454b73966bb4c4` 在完成 Skill 后超时且无最终文本，因此第二项目不能复现同一结果。3/3 回滚通过。

### `gtm-strategy` — Partial

- 3/3 精确发现。项目 B `call_10fee3681dae47a4a94387c8` 唯一一次完成 Skill 并通过核心结构。
- 项目 A 产生两个完成 Skill 事件（`call_e6d9c3c239b24021934c0730`、`call_d9640b00e4ae4f72a9b3fe66`）后超时，无最终文本，不满足唯一事件和加载断言；个人级 `call_25a31b1a3e69499e854f1681` 在一次完成事件后超时。
- 3/3 回滚通过。

### `identify-assumptions-existing` — Partial

- 项目 A `call_ecac32a116564a30b5fea272` 在一次完成 Skill 后超时；项目 B 出现两个完成事件（`call_45b32b0eb7fb44ae9df61d16`、`call_3bc5fac230d34c8c84bcb561`）后超时。个人级 `call_7e1bef86bc5c4eddb58475c7` 完整通过。
- 三范围均精确发现，只有 2/3 满足唯一目标加载；两个项目都未产生最终文本，代表能力不能复现。3/3 回滚通过。

### `identify-assumptions-new` — Partial

- 3/3 精确加载。项目 A `call_90f64690dd4b4b3dbfb85e45` 与项目 B `call_354500a8bb604f13bb212fe3` 都在完成 Skill 后超时且无最终文本。
- 个人级 `call_5c36bb6bce554d7eb6cb1e00` 未超时并覆盖 8 类风险与 30 天计划，但漏掉明确的 `ENGINEER-PERSPECTIVE` 标记，还引入需要另行核实的具体法规判断。核心门槛不通过。
- 3/3 回滚通过。

### `job-stories` — Partial

- 3/3 精确加载。项目 A `call_12f179a01d884ec19527fcdb` 只使用目标 Skill，完成 4 个 Job Stories 和验收条件。
- 项目 B 在 `call_39408415e10141aeabab3f16` 后尝试 `write`，事件 `call_58f2cdc5dd574b0480515b06` 为 `write:error`；个人级同样在 `call_e8fd83513b02416da29d3d50` 后出现 `call_7219c0847fda47d7b00c32ed` 的 `write:error`。两者都违反“只用 Skill、不写文件”的验收条件，且没有文件可作为成功证据；个人级还漏 `PRIVACY` 标记。
- 3/3 回滚通过。

### `marketing-ideas` — Partial

- 三范围的机读结构、唯一完成 Skill 事件和标记最初均通过：`call_7e057d7ce9344eb8b84df1ce`、`call_dbb7adefefb246f5bef79c08`、`call_fcc49e6d24f64326bdb46016`。
- 人工语义审计撤销通过：项目 A 把 `356 USD` 写成“10% contingency”，但其余列合计为 `1,356 USD`，10% 应约为 `135.60 USD`；个人级把未提供、未联网验证的“港铁平均 40 分钟”和法律意识写成事实；项目 B 也把产品“never phones home”等未提供能力写成事实。
- 这些错误涉及预算与营销承诺，不能只因标记齐全就发布正式指南。3/3 回滚通过。

## 回滚与基线

- 首轮 30 次与发布流 3 次，共 33/33 次卸载后都再次运行真实 `codearts debug skill`；旧绝对路径全部消失，所有项目与个人级 Skill/vendor 目标均不存在。
- 用户根 `package.json` 前后均为 SHA-256 `589A2F93D6B401FCCE4EDED58C4C477C3891C3B580796862C58ACB4E5D73A4E7`。
- 用户根 `codearts_cli.json` 前后均为 SHA-256 `6F08A23430F657E3CBF75ABDCF1CB7A08FE88504F6FDC51BA31B6530386E2E23`。
- `.tmp` 只保留忽略的审计证据，不纳入 Git 提交。

## 发布边界

本批不新增任何正式指南或根索引。10 项只能说明固定内容可被 CodeArts 原生安装和发现，或在部分范围可调用；不能宣称跨范围稳定 Works。

结论只覆盖 Windows 11、CodeArts CLI 26.8.1、`mimo/mimo-v2.5`、MIT v2.1.0 固定提交与本页列出的离线代表流程。未验证 Linux、macOS、其他模型、联网研究、文件保存、Claude Commands 或完整插件编排。

## 来源

- [CodeArts CLI Skills 文档（中文，2026-08-28 更新）](https://support.huaweicloud.com/usermanual-cli/codeartsagent_cli_0019.html)
- [CodeArts CLI 命令文档](https://support.huaweicloud.com/intl/en-us/usermanual-cli/codeartsagent_cli_0034.html)
- [PM Skills 固定提交](https://github.com/phuryn/pm-skills/tree/18468a95b427e70e258b51389796367c6f684e7d)
- [PM Skills v2.1.0](https://github.com/phuryn/pm-skills/releases/tag/v2.1.0)
- [PM Skills Issues](https://github.com/phuryn/pm-skills/issues) 与 [Pull Requests](https://github.com/phuryn/pm-skills/pulls)
- [GitHub Agent Skills Topic](https://github.com/topics/agent-skills)
